/** 帮助文档章节内容（07 §1.3：7 个章节，结构完整，占位文案）。 */

export interface HelpSection {
  id: string;
  title: string;
  markdown: string;
}

export const HELP_SECTIONS: HelpSection[] = [
  {
    id: 'quickstart',
    title: '快速开始',
    markdown: `## 快速开始

1. **导入 FAQ 模板**：进入「知识库」→ 创建知识库 → 上传 \`FAQ知识库导入模板.xlsx\`（问题 / 答案 / 分类 / 标签），等待状态变为“已完成”。
2. **验证检索**：进入「检索测试」，选择知识库并输入示例问题，切换 向量 / 混合 / 混合+重排 查看召回结果。
3. **接入客服**：进入「智能客服」，发送咨询问题体验流式回答；投诉或连续无法回答将自动转人工并生成工单。

> 默认管理员账号：\`admin / admin123\`（仅本地开发，生产环境请修改）。`,
  },
  {
    id: 'knowledge',
    title: '知识库使用指南',
    markdown: `## 知识库使用指南

- **创建知识库**：名称唯一（≤50 字），可填写描述。
- **上传文档**：支持 \`.xlsx / .csv / .md / .txt / .pdf / .docx\`，单文件 ≤ 20MB。
- **FAQ 模板**：列为 \`问题 | 答案 | 分类（可选）| 标签（可选，逗号分隔）\`，逐行生成 Chunk。
- **文本分块**：普通文本按“分块长度 500 字 / 重叠 50 字”切分，可在「系统设置 → 数据管理」调整。
- **Chunk 管理**：进入文档详情可新增 / 编辑 / 删除 Chunk；编辑后自动重新向量化。`,
  },
  {
    id: 'chat',
    title: '智能客服说明',
    markdown: `## 智能客服说明

- **意图路由**：订单/物流 → 工具查询（MVP mock）；政策类 → 知识库检索；投诉/转人工 → 建单；其他 → 兜底。
- **转人工规则**：用户投诉、明确要求转人工，或连续兜底达到阈值（默认 2 次）时创建工单，会话置为已转人工。
- **引用反馈**：回答右侧引用卡片支持 删除 / 标记无效 / 补充引用，反馈写入评测候选。
- **对话内表单**：订单查询缺少订单号时，AI 会下发表单卡片（订单号 15 位 / 联系方式校验），提交后作为上下文继续对话。`,
  },
  {
    id: 'retrieval',
    title: '检索测试使用',
    markdown: `## 检索测试使用

- **检索方式**：向量检索（pgvector 余弦）、混合检索（向量 + 全文 RRF 融合，默认）、混合+重排（SiliconFlow bge-reranker）。
- **相关度解读**：检索分 0–100（min-max 归一化）；重排分为服务商原始分数。高分绿 / 中分橙 / 低分红。
- **重排降级**：未配置重排模型时自动降级为混合检索，页面有提示。
- **标签过滤**：可按标签筛选召回，仅返回含所选标签的 Chunk。`,
  },
  {
    id: 'evaluation',
    title: '应用评测指南',
    markdown: `## 应用评测指南

- **评测集**：创建评测集后可逐条 / JSON 批量导入样本，或一键导入内置 30 条公开样例（电商售后 FAQ）。
- **评测任务**：选择评测集 + 对话模型 + 检索知识库后异步执行，逐条走对话链路生成回答并打分。
- **指标**：回答准确性（先行实现）；问题相关性、语义准确性预留。
- **报告**：平均分、通过率、样本明细；支持人工调整通过状态与 CSV 导出。
- **回流**：引用反馈与会话标注标记“纳入评测集”后进入候选，管理员确认后加入评测集。`,
  },
  {
    id: 'sessions',
    title: '会话记录与标注指南',
    markdown: `## 会话记录与标注指南

- **列表筛选**：时间范围、意图、状态、是否转人工、关键词、标注状态组合查询。
- **详情**：左侧消息流（含引用编号），右侧知识引用面板、调用链路（intent → retrieval → generate）、人工标注区与关联工单。
- **人工标注**：填写标签 / 备注；勾选“纳入评测集”可指定目标评测集，保存后样本进入评测候选。
- **关联工单**：转人工会话展示工单号，可跳转工单页处理。`,
  },
  {
    id: 'faq',
    title: 'FAQ',
    markdown: `## FAQ

**Q：上传 xlsx 后一直“解析中”？**
解析为异步任务；刷新页面即可看到最新状态，失败可点“重新解析”。

**Q：检索测试“混合+重排”为什么自动降级？**
未配置启用的重排 Profile（或 RERANK_API_KEY 未配置）时，系统自动降级为混合检索。

**Q：转人工后还能继续对话吗？**
不能。会话转人工后 AI 不再接管，需新建会话。

**Q：评测通过率为什么偏低？**
与知识库覆盖度有关：30 条公开样例覆盖范围大于示例知识库；可人工调整通过状态或扩充知识库。`,
  },
];

